[project]
name = "n-gs-pay-math-lab"
version = "0.01"
description = "Static General Schedule pay math reconstruction and verification lab."
requires-python = ">=3.11"

[tool.unittest]
start-dir = "tests"
pattern = "test_*.py"
